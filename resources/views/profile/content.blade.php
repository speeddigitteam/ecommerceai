<div class="mb-6 flex flex-col gap-5 rounded-2xl border bg-white p-5 sm:flex-row sm:items-center sm:justify-between sm:p-7"><div class="flex min-w-0 items-center gap-4">@if($user->profile_image_path)<img src="{{ asset('storage/'.$user->profile_image_path) }}" alt="{{ $user->name }}" class="h-16 w-16 rounded-2xl object-cover">@else<div class="grid h-16 w-16 place-items-center rounded-2xl bg-indigo-600 text-2xl font-bold text-white">{{ strtoupper(substr($user->name,0,1)) }}</div>@endif<div class="min-w-0"><p class="text-sm font-semibold text-indigo-600">Account settings</p><h1 class="mt-1 truncate text-2xl font-bold">{{ $user->name }}</h1><p class="truncate text-sm text-slate-500">{{ $user->email }}</p></div></div><span class="w-fit rounded-full bg-emerald-50 px-3 py-1.5 text-xs font-semibold text-emerald-700">Active account</span></div>
<div class="grid items-start gap-6 {{ $user->isAdmin() ? 'xl:grid-cols-2' : '' }}"><section class="rounded-2xl border bg-white p-5 shadow-sm sm:p-7"><div class="mb-6 border-b pb-5"><p class="font-bold">Personal details</p><p class="text-xs text-slate-500">Manage your profile information.</p></div>@include('profile.partials.update-profile-information-form')</section>@if($user->isAdmin())<section id="password-security" class="scroll-mt-24 rounded-2xl border bg-white p-5 shadow-sm sm:p-7"><div class="mb-6 border-b pb-5"><p class="font-bold">Account security</p><p class="text-xs text-slate-500">Keep your password secure and current.</p></div>@include('profile.partials.update-password-form')</section>@endif</div>
<section class="mt-6 rounded-2xl border border-rose-200 bg-rose-50/50 p-5 sm:p-7">@include('profile.partials.delete-user-form')</section>
