<x-admin-layout title="New Order">
    <div class="min-h-screen bg-slate-50 text-slate-900 dark:bg-[#111827] dark:text-slate-100">
        <div x-cloak x-show="menuOpen" @click="menuOpen = false" class="fixed inset-0 z-30 bg-slate-950/50 lg:hidden"></div><x-admin-sidebar />
        <main class="min-w-0 lg:pl-72"><x-admin-topbar /><div class="mx-auto max-w-3xl p-5 sm:p-8"><a href="{{ route('orders.index') }}" class="text-sm font-semibold text-indigo-600 dark:text-indigo-400">← Back to orders</a>
            <section class="ds-card mt-5 overflow-hidden shadow-sm"><div class="border-b border-sky-100 bg-sky-50 px-5 py-5 dark:border-sky-500/20 dark:bg-sky-500/10 sm:px-7"><p class="text-xs font-semibold uppercase tracking-wider text-sky-600 dark:text-sky-400">Manual order</p><h1 class="mt-1 text-2xl font-bold">Create new order</h1><p class="mt-1 text-sm text-slate-500">Add a customer order from the dashboard.</p></div>
                <form x-data="{ selectedProduct: @js((string) old('product_id', '')), rates: @js($products->mapWithKeys(fn ($product) => [$product->id => app(App\Services\DeliveryCharges::class)->rates([$product])])), shippingRate(area) { return this.rates[this.selectedProduct]?.[area] ?? 0; } }" method="POST" action="{{ route('orders.store') }}">@csrf<div class="grid gap-5 p-5 sm:grid-cols-2 sm:p-7"><div class="sm:col-span-2"><label for="product_id" class="ds-field-label">Product</label><select id="product_id" name="product_id" x-model="selectedProduct" class="ds-select ds-control" required><option value="">Select product</option>@foreach ($products as $product)<option value="{{ $product->id }}" @selected(old('product_id') == $product->id)>{{ $product->title }} — ৳{{ number_format((float) $product->current_price, 2) }} ({{ $product->stock_quantity }} in stock)</option>@endforeach</select>@error('product_id')<p class="ds-error">{{ $message }}</p>@enderror</div><div><label for="quantity" class="ds-field-label">Quantity</label><input id="quantity" type="number" min="1" name="quantity" value="{{ old('quantity', 1) }}" class="ds-input ds-control" required>@error('quantity')<p class="ds-error">{{ $message }}</p>@enderror</div><div><label for="customer_name" class="ds-field-label">Customer name</label><input id="customer_name" name="customer_name" value="{{ old('customer_name') }}" class="ds-input ds-control" required>@error('customer_name')<p class="ds-error">{{ $message }}</p>@enderror</div><div><label for="customer_phone" class="ds-field-label">Phone</label><input id="customer_phone" name="customer_phone" value="{{ old('customer_phone') }}" class="ds-input ds-control" required>@error('customer_phone')<p class="ds-error">{{ $message }}</p>@enderror</div><div><label for="customer_email" class="ds-field-label">Email</label><input id="customer_email" type="email" name="customer_email" value="{{ old('customer_email') }}" class="ds-input ds-control">@error('customer_email')<p class="ds-error">{{ $message }}</p>@enderror</div><div><label for="delivery_area" class="ds-field-label">Delivery area</label><select id="delivery_area" name="delivery_area" class="ds-select ds-control" required><option value="dhaka_city" @selected(old('delivery_area') === 'dhaka_city') x-text="'Dhaka City — ' + shippingRate('dhaka_city').toFixed(2) + ' TK'">Dhaka City</option><option value="dhaka_outside" @selected(old('delivery_area') === 'dhaka_outside') x-text="'Outside Dhaka City — ' + shippingRate('dhaka_outside').toFixed(2) + ' TK'">Outside Dhaka City</option><option value="outside_dhaka" @selected(old('delivery_area') === 'outside_dhaka') x-text="'Outside Dhaka District — ' + shippingRate('outside_dhaka').toFixed(2) + ' TK'">Outside Dhaka District</option></select></div><div class="sm:col-span-2"><label for="shipping_address" class="ds-field-label">Delivery address</label><textarea id="shipping_address" name="shipping_address" rows="4" class="ds-textarea ds-control" required>{{ old('shipping_address') }}</textarea>@error('shipping_address')<p class="ds-error">{{ $message }}</p>@enderror</div><div class="sm:col-span-2"><label for="customer_note" class="ds-field-label">Customer note</label><textarea id="customer_note" name="customer_note" rows="3" class="ds-textarea ds-control">{{ old('customer_note') }}</textarea></div></div><div class="flex justify-end gap-3 border-t border-slate-200 bg-slate-50/70 px-5 py-4 dark:border-slate-700 dark:bg-slate-900/20 sm:px-7"><a href="{{ route('orders.index') }}" class="ds-button-secondary inline-flex items-center px-4">Cancel</a><button class="ds-button-primary px-5">Create order</button></div></form>
            </section>
        </div></main>
    </div>
</x-admin-layout>
