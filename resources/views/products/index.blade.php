<x-admin-layout title="Products">
    <div class="min-h-screen bg-slate-50 text-slate-900 dark:bg-[#111827] dark:text-slate-100">
        <div x-cloak x-show="menuOpen" @click="menuOpen = false" class="fixed inset-0 z-30 bg-slate-950/50 lg:hidden"></div><x-admin-sidebar />
        <main class="min-w-0 lg:pl-72"><x-admin-topbar /><div class="mx-auto max-w-7xl p-5 sm:p-8">
            <div class="mb-6 flex items-end justify-between gap-4"><div><p class="text-sm font-semibold text-indigo-600 dark:text-indigo-400">Catalog</p><h1 class="mt-1 text-2xl font-bold">Products list</h1><p class="mt-2 text-sm text-slate-500">Manage your ecommerce products and inventory.</p></div><a href="{{ route('products.create') }}" class="inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white"><svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-width="2" d="M12 5v14M5 12h14"/></svg>Add new product</a></div>
            @if (session('status'))<div class="mb-6 rounded-xl bg-emerald-50 px-4 py-3 text-sm font-medium text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300">{{ session('status') }}</div>@endif
            <section class="overflow-hidden rounded-2xl border border-slate-200 bg-white dark:border-slate-800 dark:bg-[#161f2e]">
                <div class="overflow-x-auto"><table class="w-full min-w-[900px] text-left text-sm"><thead class="bg-slate-50 text-xs uppercase tracking-wider text-slate-500 dark:bg-slate-800/60"><tr><th class="px-5 py-4">Product</th><th class="px-5 py-4">SKU</th><th class="px-5 py-4">Price</th><th class="px-5 py-4">Stock</th><th class="px-5 py-4">Category</th><th class="px-5 py-4">Status</th><th class="px-5 py-4 text-right">Actions</th></tr></thead><tbody class="divide-y divide-slate-100 dark:divide-slate-800">
                    @forelse ($products as $product)<tr class="hover:bg-slate-50/70 dark:hover:bg-slate-800/30"><td class="px-5 py-4"><div class="flex items-center gap-3">@if ($product->featured_image_path)<img src="{{ asset('storage/'.$product->featured_image_path) }}" alt="" class="h-12 w-12 rounded-xl object-cover">@else<div class="grid h-12 w-12 place-items-center rounded-xl bg-slate-100 text-slate-400 dark:bg-slate-800"><svg class="h-5 w-5" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M4 5h16v14H4zM4 16l4-4 3 3 2-2 3 3"/></svg></div>@endif<div><p class="font-semibold">{{ $product->title }}</p><p class="mt-0.5 text-xs text-slate-400">/{{ $product->slug }}</p></div></div></td><td class="px-5 py-4 text-slate-500">{{ $product->sku ?: '—' }}</td><td class="px-5 py-4 font-semibold">{{ $product->price !== null ? number_format((float) ($product->sale_price ?? $product->price), 2) : '—' }}</td><td class="px-5 py-4">@if ($product->isDigital())<span class="rounded-full bg-violet-50 px-2.5 py-1 text-xs font-semibold text-violet-700 dark:bg-violet-500/15 dark:text-violet-300">Digital</span>@else<span class="{{ $product->stock_quantity > 0 ? 'text-emerald-600' : 'text-rose-600' }}">{{ $product->stock_quantity }}</span>@endif</td><td class="px-5 py-4 text-slate-500">{{ $product->category?->name ?? '—' }}</td><td class="px-5 py-4"><span class="rounded-full px-2.5 py-1 text-xs font-semibold {{ $product->status === 'published' ? 'bg-emerald-50 text-emerald-700 dark:bg-emerald-500/15 dark:text-emerald-300' : 'bg-amber-50 text-amber-700 dark:bg-amber-500/15 dark:text-amber-300' }}">{{ ucfirst($product->status) }}</span></td><td class="px-5 py-4"><div class="flex justify-end"><div x-data="{ open: false, top: 0, left: 0, toggle() { if (this.open) { this.open = false; return; } const rect = this.$refs.trigger.getBoundingClientRect(); const menuHeight = 190; this.top = window.innerHeight - rect.bottom >= menuHeight ? rect.bottom + 8 : Math.max(8, rect.top - menuHeight); this.left = Math.max(12, Math.min(rect.right - 176, window.innerWidth - 188)); this.open = true; } }" @keydown.escape.window="open = false" @scroll.window="open = false" @resize.window="open = false"><button x-ref="trigger" type="button" @click.stop="toggle()" :aria-expanded="open" aria-label="Product actions" class="grid h-9 w-9 place-items-center rounded-lg text-slate-500 transition hover:bg-slate-100 hover:text-slate-900 focus:outline-none focus:ring-2 focus:ring-indigo-500 dark:hover:bg-slate-800 dark:hover:text-white"><svg class="h-5 w-5" viewBox="0 0 24 24" fill="currentColor"><circle cx="12" cy="5" r="1.75"/><circle cx="12" cy="12" r="1.75"/><circle cx="12" cy="19" r="1.75"/></svg></button><template x-teleport="body"><div x-cloak x-show="open" x-transition.opacity @click.outside="open = false" :style="`top: ${top}px; left: ${left}px`" class="fixed z-[100] w-44 overflow-hidden rounded-xl border border-slate-200 bg-white py-1.5 shadow-xl dark:border-slate-700 dark:bg-slate-800"><a href="{{ route('products.show', $product) }}" class="block px-3.5 py-2 text-sm hover:bg-slate-50 dark:hover:bg-slate-700">Preview</a><a href="{{ route('products.edit', $product) }}" class="block px-3.5 py-2 text-sm hover:bg-slate-50 dark:hover:bg-slate-700">Edit</a><form method="POST" action="{{ route('products.duplicate', $product) }}">@csrf<button type="submit" class="block w-full px-3.5 py-2 text-left text-sm hover:bg-slate-50 dark:hover:bg-slate-700">Duplicate</button></form><div class="my-1 border-t border-slate-100 dark:border-slate-700"></div><form method="POST" action="{{ route('products.destroy', $product) }}" onsubmit="return confirm('Delete this product?')">@csrf @method('DELETE')<button type="submit" class="block w-full px-3.5 py-2 text-left text-sm text-rose-600 hover:bg-rose-50 dark:text-rose-400 dark:hover:bg-rose-500/10">Delete</button></form></div></template></div></div></td></tr>
                    @empty<tr><td colspan="7" class="p-12 text-center"><p class="text-slate-500">No products yet.</p><a href="{{ route('products.create') }}" class="mx-auto mt-4 inline-flex items-center gap-2 rounded-xl bg-indigo-600 px-4 py-2.5 text-sm font-semibold text-white"><svg class="h-4 w-4" fill="none" stroke="currentColor" viewBox="0 0 24 24"><path stroke-width="2" d="M12 5v14M5 12h14"/></svg>Add new product</a></td></tr>@endforelse
                </tbody></table></div>@if ($products->hasPages())<div class="border-t border-slate-200 px-5 py-4 dark:border-slate-800">{{ $products->links() }}</div>@endif
            </section>
        </div></main>
    </div>
</x-admin-layout>
